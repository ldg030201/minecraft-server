# RCON 비밀번호 (필수)
RCON_PASSWORD=changeme

# === 로컬 개발용 (친구 PC에서는 주석 처리) ===
# 아래 주석 해제하면 GHCR 대신 로컬에서 빌드한 이미지를 씁니다.
# 사용 순서:
#   1) docker compose build          (로컬 이미지 빌드)
#   2) docker compose up -d           (로컬 이미지로 실행)
#
# TEG_ADMIN_IMAGE=teg-admin:local
# TEG_ADMIN_PULL=never
# MC_IMAGE=itzg/minecraft-server:java11
# MC_PULL=missing
